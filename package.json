{
  "name": "bank-retention-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Root build wrapper for the React frontend. Node/npm are used only to build static assets consumed by the Python (FastAPI) backend; they are not part of the runtime.",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm run install:frontend && npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
