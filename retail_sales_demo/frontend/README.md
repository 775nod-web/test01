# retail_sales_demo フロントエンド

TypeScript + React (Vite) 製の小売POS売上分析デモSPA。詳細は
`../docs/phase2_frontend.md` を参照。

```bash
npm install
npm run dev     # http://localhost:5173、/api は :8000 のFastAPIへプロキシ
npm run build   # ../app/static に出力（Databricks Apps側でFastAPIが配信）
```
