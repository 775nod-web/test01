# PHASE_PROMPTS.md

以下のプロンプトを、Phase 0から順番にClaude Codeへ渡してください。  
各フェーズで、Claude Codeには`CLAUDE.md`と`DEMO_SPEC.md`を必ず参照させてください。

---

# Phase 0: 現状確認と実装計画

```text
CLAUDE.mdとDEMO_SPEC.mdを最初に全文確認してください。

このフェーズでは、まだアプリ機能を実装しないでください。リポジトリの現状を調査し、Databricks Apps上で動かすための実装計画を作成してください。

目的:
- Databricks Free Editionで動作する、TypeScript + ReactフロントエンドとPython FastAPIバックエンドの単一Databricks Appを設計する。
- UI serving layerのみを実装し、パイプライン、モデル学習、モデルサービング、外部APIを作らない。
- Reactを静的ビルドし、FastAPIから同一オリジンで配信する。

実施事項:
1. 現在のファイル構成と既存コードを確認する。
2. CLAUDE.mdの絶対条件と禁止事項に対して、矛盾がないか確認する。
3. 採用する依存関係を最小限で提案する。
4. package.json、requirements.txt、vite.config.ts、app.yaml、Pythonエントリポイントの構成を設計する。
5. Databricks Appsのビルドと起動フローを説明する。
6. API、Reactコンポーネント、合成データのファイル分割を提示する。
7. 各フェーズの検証方法を提示する。
8. リスクと回避策を整理する。
9. PLAN.mdを作成する。

制約:
- ローカルサーバーを最終成果物にしない。
- Dockerを追加しない。
- Expressを使用しない。
- Streamlitを使用しない。
- データベースを追加しない。
- 実装していないDatabricks機能を依存関係にしない。
- このフェーズでは大規模なコード生成を行わない。

終了時の報告:
- 調査したファイル
- 作成または更新したファイル
- 採用アーキテクチャ
- Databricks Appsデプロイまでの手順
- 次フェーズの作業範囲
- 未解決事項
```

---

# Phase 1: アプリ基盤と合成データAPI

```text
CLAUDE.md、DEMO_SPEC.md、PLAN.mdを確認し、Phase 1を実行してください。

このフェーズの目的は、Databricks Appsで起動可能な最小アプリ基盤と、合成データを返すPython APIを完成させることです。ダッシュボードの完成UIはまだ作り込まないでください。

実施事項:
1. TypeScript + React + Viteのフロントエンド基盤を作成する。
2. Python FastAPIのバックエンドを作成する。
3. Reactのビルド成果物をFastAPIから配信する。
4. app.yamlを作成し、DATABRICKS_APP_PORTでUvicornを起動する。
5. package.jsonにbuildスクリプトを定義する。
6. requirements.txtを最小構成で作成する。
7. 以下のAPIを実装する。
   - GET /api/health
   - GET /api/dashboard
   - GET /api/cases
   - GET /api/cases/{transaction_id}
   - POST /api/cases/{transaction_id}/decision
8. Pydanticモデルを定義する。
9. DEMO_SPEC.mdに合う日本語合成データを作成する。
10. rulesとhybrid、7dと30d、各チャネルで一貫したレスポンスを返す。
11. 404、422、内部エラーの扱いを実装する。
12. React側には、API疎通確認ができる最小画面を作る。
13. ローディングとエラー表示を最低限実装する。
14. README.mdの初版を作成する。Databricks Apps上でのデプロイ方法を中心に記載する。

重要:
- データは固定PythonデータまたはJSONだけを使用する。
- Spark、SQL Warehouse、Delta、MLflow、Model Servingを使わない。
- POSTの更新はメモリ内だけでよい。
- Node.jsはビルドツールとしてのみ使用し、バックエンドには使用しない。
- npmの実行環境がなくても、コードと設定の静的整合性を確認する。
- 利用可能ならビルドとPythonテストを実行する。
- app.yamlのポート指定をDatabricks Apps向けに確認する。

受け入れ条件:
- FastAPIアプリが静的Reactを配信する構成になっている。
- 全APIの正常系と主要異常系が確認できる。
- 合成データに個人情報がない。
- 代表ケースTXN-100123が存在する。
- 依存関係が過剰でない。
- READMEにDatabricks Appsでのデプロイ手順がある。

終了時の報告:
- 変更ファイル一覧
- API一覧
- 実行したテスト
- テスト結果
- Databricks Apps上で必要な操作
- 残課題
```

---

# Phase 2: Fraud Command Center

```text
CLAUDE.md、DEMO_SPEC.md、PLAN.mdを確認し、Phase 2を実行してください。

このフェーズでは、経営・リスク責任者向けのFraud Command Centerを完成させてください。

実施事項:
1. Google流の淡く鮮やかな配色と余白を用いた、日本語ダッシュボードを作成する。
2. グローバルナビゲーションを作成する。
3. 合成データ利用の注記を常時表示する。
4. 主要4 KPIを大きく表示する。
   - 推定防止損失額
   - 正常取引承認率
   - 誤検知率
   - 平均検知時間
5. その他のKPIを整理して表示する。
6. 以下の操作を実装する。
   - 期間: 7日、30日
   - 判定方式: 既存ルールのみ、ルール＋AI
   - チャネル: すべて、モバイル決済、デビットカード
7. シナリオ切替でKPI、比較カード、グラフ、一覧が連動するようにする。
8. 既存ルールのみとルール＋AIの比較カードを作成する。
9. 以下の可視化を実装する。
   - 不正率の時系列
   - 正常承認率の時系列
   - 不正損失額と推定防止損失額
   - リスクスコア分布
10. 高リスク取引一覧を実装する。
11. TXN-100123を先頭に表示する。
12. 行クリックで調査詳細へ遷移できるようにする。
13. KPIとグラフに日本語の説明ツールチップまたは補足文を付ける。
14. ローディング、エラー、空状態を整える。
15. 1366×768で主要メッセージが見えるようにする。
16. レスポンシブ対応を行う。
17. READMEに「概況画面の使い方」を追記する。

実装上の注意:
- チャートライブラリは軽量なものを1つだけ使用する。
- 二軸グラフは避ける。
- 色だけで良否を伝えない。
- 数値を過剰に好転させない。
- 「AIで不正を確定」の表現を使わない。
- 技術機能名を画面の主役にしない。
- KPIカードには業務上の意味を短く表示する。

受け入れ条件:
- 30秒以内に画面の目的を理解できる。
- フィルター操作が表示へ反映される。
- ルール＋AIの価値が、不正捕捉だけでなく正常顧客保護として伝わる。
- 高リスク取引から調査画面へ移動できる。
- コンソールエラーがない。
- 主要操作がキーボードで可能。

終了時の報告:
- 変更ファイル一覧
- 実装した画面と操作
- 使用したチャートライブラリ
- 実行した検証
- 残課題
```

---

# Phase 3: Investigation Workbenchとデモガイド

```text
CLAUDE.md、DEMO_SPEC.md、PLAN.mdを確認し、Phase 3を実行してください。

このフェーズでは、調査担当者向けInvestigation Workbenchと10分デモガイドを完成させてください。

実施事項:
1. ケース一覧画面を実装する。
2. 優先度、ステータス、推奨アクション、リスク帯、検索のフィルターを実装する。
3. ケース詳細画面を実装する。
4. TXN-100123の詳細に以下を表示する。
   - 取引サマリー
   - 主要リスク要因
   - 顧客の通常行動との差
   - 直近24時間の取引タイムライン
   - 直近7日間の要約
   - 関連カード、端末、口座、加盟店
   - 既存ルール該当理由
   - AIスコア上位要因
   - モデルバージョン
   - 判定日時
   - データ更新時刻
   - 操作履歴
5. 「リスクスコアは不正を確定するものではない」という説明を明示する。
6. 調査結果として、不正、正常、追加確認を登録できるようにする。
7. 任意の調査メモを入力できるようにする。
8. 登録後にトースト、ステータス更新、操作履歴追加を行う。
9. セッション内のみ保持されることを表示する。
10. デモガイドを実装する。
11. 10分の操作順と話すメッセージを日本語で表示する。
12. 概況画面へ戻れる導線を用意する。
13. READMEに調査画面とデモガイドの使い方を追記する。

実装上の注意:
- 個人名、完全なカード番号、連絡先を表示しない。
- 関連情報は簡易リストとし、グラフ分析を実装しない。
- 「監査完備」と断定しない。
- Databricksの将来連携は注記に留める。
- 調査結果登録は実運用の決済制御ではないことを明示する。
- モーダルを多用せず、詳細情報を読みやすく配置する。

受け入れ条件:
- ケース一覧から詳細へ移動できる。
- 主要要因を見て判断理由を理解できる。
- 調査結果を登録できる。
- 登録後の状態が画面へ反映される。
- 10分デモをガイドどおり再現できる。
- すべて日本語で表示される。
- アクセシビリティ上の重大な問題がない。

終了時の報告:
- 変更ファイル一覧
- 完成したユーザーフロー
- 状態更新の仕組み
- 実行した検証
- 残課題
```

---

# Phase 4: 品質改善、Databricks Appsデプロイ、最終化

```text
CLAUDE.md、DEMO_SPEC.md、PLAN.md、README.mdを確認し、Phase 4を実行してください。

このフェーズの目的は、デモ品質を高め、Databricks Apps上で起動確認し、他の人が再現できる状態にすることです。

実施事項:
1. 全画面の文言を日本語として校正する。
2. 経営層向けのメッセージが技術用語より先に伝わるか確認する。
3. 画面間のKPI、ケース、金額、日時の整合性を確認する。
4. TypeScriptの型エラー、Pythonの構文エラー、APIエラーを解消する。
5. ビルドを実行する。
6. 可能な範囲でAPIテストを実行する。
7. 主要ユーザーフローを確認する。
   - 概況表示
   - ルールからハイブリッドへの切替
   - 期間とチャネル変更
   - TXN-100123を開く
   - 調査結果登録
   - デモガイド閲覧
8. ローディング、空状態、API障害時表示を確認する。
9. キーボード操作とフォーカス表示を確認する。
10. 1366×768と一般的なノートPC幅で確認する。
11. app.yaml、requirements.txt、package.json、ビルド出力先を最終確認する。
12. Databricks Appsへデプロイする。
13. Databricks Apps上のURLで起動確認する。
14. 起動ログ、API疎通、静的アセット配信を確認する。
15. Databricks Appsでのみ発生する問題を修正して再デプロイする。
16. READMEを最終化する。

READMEに必ず含める内容:
- デモの目的
- 対象ユーザー
- 実装範囲
- 実装していない範囲
- 技術構成
- ファイル構成
- Databricks Free Edition上の前提
- Databricks Appsの作成方法
- ソースの配置方法
- デプロイ方法
- 再デプロイ方法
- アプリの起動方法
- アプリの使い方
- 10分デモ手順
- トラブルシューティング
- 合成データとセッション内状態の注意
- 将来の本番構成との違い

重要:
- ローカルで動作しただけでは完了にしない。
- Databricks Apps上での動作確認を完了条件とする。
- デプロイ権限や環境上の理由で実行できない場合は、成功したと記載しない。
- その場合、実施済み内容、正確なエラー、必要な利用者操作をREADMEと最終報告に記録する。
- 新機能追加より、安定性、読みやすさ、デモ再現性を優先する。
- パイプラインやモデルを追加しない。

最終受け入れ条件:
- Databricks Apps上でアプリが起動する。
- React画面とFastAPIが同一アプリで動作する。
- UIが日本語である。
- 主要デモフローが再現できる。
- 外部サービスなしで動作する。
- Free Edition向けに軽量である。
- READMEだけで別担当者がデプロイとデモを再現できる。
- 実装範囲外の機能を誤認させない。

終了時の最終報告:
- 完成した機能
- 変更ファイル一覧
- Databricks Appsのデプロイ結果
- 実行したテストと結果
- 既知の制約
- READMEの場所
- デモURL。取得できた場合のみ記載
```
