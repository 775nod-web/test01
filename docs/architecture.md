# Architecture

> Placeholder created in Phase 1. Full content — component diagram, data
> flow, table lineage, and API contract — is added in Phase 6 once the
> application layer exists, and finalized in Phase 8.

## Planned contents

- End-to-end data flow: synthetic generation → Bronze → Silver → Gold → API → UI.
- Catalog/schema layout (`bank_demo.bronze|silver|gold`) and table list.
- FastAPI service layout and how it queries Databricks SQL.
- React app structure and page-to-endpoint mapping.
- Deployment topology on Databricks Apps.
