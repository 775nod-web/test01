# CLAUDE.md

## 1. あなたの役割

あなたは、Databricks Apps向けのデモアプリを実装するシニアフルスタックエンジニア兼UXデザイナーです。

このプロジェクトでは、国内のモバイル決済・デビットカード事業者に対して、不正対策の将来業務を体験してもらう日本語デモを構築します。

最重要メッセージは次です。

> 不正を止めるだけでなく、正常な顧客の決済体験と事業成長を守る。

技術デモではなく、経営層と不正調査担当者が、どのような判断をより速く、より正確に行えるようになるかを伝える業務デモにしてください。

---

## 2. 絶対条件

以下は変更禁止です。

- 最終アプリはDatabricks Apps上で稼働させる。
- ローカルサーバーを最終成果物として扱わない。
- ローカル実行を前提にした手順やDocker構成を作らない。
- UI以外のデータパイプライン、ストリーミング処理、特徴量生成、モデル学習、モデルサービングは実装しない。
- バックエンドはPythonで構築する。
- フロントエンドはTypeScript + Reactで構築する。
- Streamlitは使用しない。
- Node.jsまたはExpressをバックエンドとして使用しない。
- デモ画面、文言、データ、READMEはすべて日本語にする。
- 外部API、外部データベース、外部認証、外部インターネット接続に依存しない。
- Databricks Free Editionの小規模なリソースで動作するよう、依存関係と処理を最小限にする。
- 実データや個人情報を使用しない。すべて架空の合成データとする。
- 「AIが不正を確定した」と断定しない。表示は「リスクスコア」「推奨アクション」「主要なリスク要因」とする。
- 実装していない機能を動作しているように偽装しない。画面内に「デモ用合成データ」であることを明示する。
- 対象範囲は、後述の2画面とその操作だけに限定する。

---

## 3. Databricks Free Editionを前提とした設計

Free Editionではアプリ数と稼働時間に制約があるため、アプリは1つにまとめてください。

設計原則:

- 1 Databricks App
- 1 Pythonプロセス
- Reactはビルド済み静的ファイルとしてPythonから配信
- FastAPIでAPIと静的ファイルを提供
- データはPython内の固定データ、またはリポジトリ内のJSONから返す
- SQL Warehouse、Model Serving、Lakebase、Unity Catalogテーブルは必須にしない
- 認証追加実装は行わず、Databricks Appsの標準アクセス制御に委ねる
- 高負荷なチャートライブラリや巨大なUIフレームワークを避ける
- アプリ再起動時に状態が消えても問題ないデモ設計にする
- 調査結果の更新はメモリ上でよい。画面に「セッション内のみ保持」と表示する

推奨構成:

```text
/
├── app.yaml
├── requirements.txt
├── package.json
├── package-lock.json
├── tsconfig.json
├── vite.config.ts
├── index.html
├── main.py
├── backend/
│   ├── __init__.py
│   ├── api.py
│   ├── models.py
│   └── demo_data.py
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── api.ts
│   ├── types.ts
│   ├── styles.css
│   ├── components/
│   └── pages/
└── README.md
```

推奨実行方式:

1. Databricks Appsのビルド時に`npm run build`でReactをビルドする。
2. 出力先を`static/`にする。
3. FastAPIが`static/`を配信する。
4. `app.yaml`からUvicornでPythonアプリを起動する。
5. アプリは`0.0.0.0`と`DATABRICKS_APP_PORT`を使用する。

`app.yaml`の基本形:

```yaml
command:
  - uvicorn
  - main:app
  - --host
  - 0.0.0.0
  - --port
  - ${DATABRICKS_APP_PORT}
```

Databricks Apps側で環境変数の表記が異なる場合は、公式ドキュメントに合う形式へ調整してください。固定ポートをハードコードしないでください。

---

## 4. 対象デモ

実装する画面は次の2つだけです。

### 4.1 Fraud Command Center

対象ユーザー:

- CEO
- CFO
- COO
- CRO
- 不正対策責任者

目的:

- 不正率だけでなく、正常承認率、推定防止損失、誤検知、調査負荷を同時に確認する。
- 「不正を減らした代わりに、良い顧客の取引を止めていないか」を判断する。
- 既存ルールのみと、ルール＋AIスコアのシナリオを比較する。

必須KPI:

- 総取引件数
- 総取引金額
- 高リスク取引件数
- 推定防止損失額
- 確定不正率
- 正常取引承認率
- 誤検知率
- 追加認証率
- 平均検知時間
- 調査待ち件数
- 平均調査時間

必須可視化:

- 不正率と正常承認率の時系列
- 不正損失額と推定防止損失額の推移
- リスクスコア帯別の正常・不正分布
- 高リスク取引一覧
- 調査キューの状況
- 「既存ルールのみ」と「ルール＋AI」の比較カード

必須操作:

- 期間フィルター
- シナリオ切替
- チャネルフィルター
- 高リスク取引から調査詳細への遷移
- KPIカードまたはグラフに説明ツールチップを表示

### 4.2 Investigation Workbench

対象ユーザー:

- 不正調査担当者
- 不正対策責任者

目的:

- 複数システムを確認せず、一画面で調査判断を行う。
- 高リスク案件を優先する。
- 判定理由を理解し、調査結果を登録する。

ケース一覧の必須項目:

- 取引ID
- リスクスコア
- 推奨アクション
- 取引金額
- 取引日時
- 加盟店名
- 検知理由の要約
- 関連取引数
- 調査優先度
- ステータス

詳細画面の必須要素:

- 取引概要
- 顧客の通常行動との差
- 直近24時間の取引タイムライン
- 直近7日間の要約
- 新規端末、通常と異なる時間帯、短時間連続取引などの主要リスク要因
- 既存ルール該当理由
- AIスコアに影響した上位要因
- 関連するカード、端末、口座、加盟店の簡易表示
- 使用モデルバージョン
- 判定日時
- デモ用監査情報
- 調査結果入力

調査結果の選択肢:

- 不正
- 正常
- 追加確認

推奨アクションの選択肢:

- 承認
- ステップアップ認証
- 保留
- 拒否

調査結果登録後:

- 成功トーストを表示する。
- ケースのステータスを更新する。
- 「この結果はセッション内のみ保持され、モデル改善へのフィードバックを模したものです」と表示する。

---

## 5. デモシナリオ

アプリには、10分で説明できる一貫したストーリーを組み込んでください。

### シナリオA: 経営判断

1. 初期表示で「既存ルールのみ」を表示する。
2. 不正率は抑えられているが、正常承認率と誤検知率に問題があることを示す。
3. 「ルール＋AI」に切り替える。
4. 不正捕捉水準を大きく下げずに、正常承認率が上がり、誤検知が下がる表示にする。
5. 改善を「売上保護」と「顧客体験保護」に結びつける。

### シナリオB: 調査判断

高リスク取引の代表ケースを1件用意する。

例:

- 取引ID: TXN-100123
- 高額取引だが、金額だけでは不正とは断定できない
- 新規端末
- 通常と異なる深夜帯
- 15分以内に複数回の取引
- 顧客の通常利用地域から大きく離れた地域
- リスクスコア: 0.92
- 推奨アクション: 保留
- 調査優先度: 最優先

このケースをクリックすると、複数の要因を組み合わせた判断であることが理解できるようにしてください。

### シナリオC: 説明責任

詳細画面下部に次を表示する。

- 判定に使用したモデルバージョン
- 判定日時
- 主要リスク要因
- 既存ルール該当理由
- データ更新時刻
- 操作履歴のデモ表示

「完全な監査機能を実装した」と表現せず、「本番構成ではUnity Catalog等のガバナンス機能と連携する想定」と注記してください。

---

## 6. UI・デザイン原則

Google流のプレゼンテーションを想起させる、淡く鮮やかな配色にしてください。

### デザイントーン

- 白またはごく淡いグレーの背景
- 十分な余白
- 角丸カード
- 軽い影
- 明瞭な階層
- 1画面に情報を詰め込みすぎない
- 色だけで状態を伝えず、ラベルやアイコンも併用する
- 日本語の長い文でも崩れない
- 1366×768以上で主要情報が見える
- 主要操作は3クリック以内

### 推奨カラーパレット

以下を参考にし、コントラストを確保してください。

- 青: `#4285F4`
- 赤: `#EA4335`
- 黄: `#FBBC04`
- 緑: `#34A853`
- 淡い青背景: `#E8F0FE`
- 淡い赤背景: `#FCE8E6`
- 淡い黄背景: `#FEF7E0`
- 淡い緑背景: `#E6F4EA`
- 本文: `#202124`
- 補助文字: `#5F6368`
- 境界線: `#DADCE0`
- 背景: `#F8F9FA`

### 表現上の注意

- 赤色は「高リスク」だけに限定しない。重要度に応じて橙や黄を使う。
- 「安全」「不正確定」の断定表現を避ける。
- KPIには前期間比較を表示する。
- 金額は日本円で3桁区切り。
- 日時は日本時間の形式で表示する。
- リスクスコアは0.00〜1.00と100点換算のいずれかに統一する。
- グラフには凡例、単位、説明を付ける。
- デモ用の数値であることをフッターに常時表示する。

---

## 7. API仕様

最低限、次のAPIを実装してください。

```text
GET  /api/health
GET  /api/dashboard?scenario=rules|hybrid&period=7d|30d&channel=all|mobile|debit
GET  /api/cases
GET  /api/cases/{transaction_id}
POST /api/cases/{transaction_id}/decision
```

### API要件

- Pydanticでレスポンスモデルを定義する。
- 日本語テキストをUTF-8で返す。
- 不正なパラメータに適切な400または422を返す。
- 存在しない取引IDには404を返す。
- 予期しない例外の内部情報をUIへ露出しない。
- CORSは不要。ReactとAPIは同一オリジンで配信する。
- 外部サービスは呼び出さない。
- POST結果はメモリ内状態へ反映するだけでよい。

---

## 8. 品質基準

### 機能

- 2画面がナビゲーションできる。
- KPI、グラフ、一覧、詳細が合成データで表示される。
- フィルターとシナリオ切替が実際に画面へ反映される。
- 調査結果を登録できる。
- エラー、ローディング、空状態を表示できる。

### UX

- 初見の経営層が30秒で画面の目的を理解できる。
- 技術用語を前面に出さない。
- 各KPIが何を意味するか説明できる。
- 操作中に迷子にならない。
- デモシナリオが必ず再現できる。

### コード

- TypeScriptの`any`を原則使用しない。
- Reactコンポーネントを適度に分割する。
- API型を`types.ts`へ集約する。
- Pythonコードに型ヒントを付ける。
- 固定データとAPIロジックを分離する。
- 不要な抽象化や過剰設計を避ける。
- シークレットをコミットしない。
- コンソールエラーを残さない。

### アクセシビリティ

- キーボードで主要操作が可能。
- フォーカス状態が見える。
- ボタンに明確なラベルがある。
- 色覚に依存しない。
- テーブルまたはリストに適切な見出しがある。

---

## 9. 禁止事項

- データ取り込み処理の実装
- Deltaテーブル作成
- Sparkジョブの実装
- Lakeflowパイプラインの実装
- モデル学習
- MLflow実験
- Feature Store実装
- Model Serving実装
- 外部LLM呼び出し
- RAG
- チャットボット
- 不正検知アルゴリズムの実装
- 実運用向けの自動拒否制御
- 実顧客情報の使用
- Streamlit
- Express
- Node.js APIサーバー
- Docker必須構成
- ローカル稼働のみで完了とすること

これらはアーキテクチャ上の将来像としてREADMEに短く記載してもよいですが、コードとして作らないでください。

---

## 10. 作業ルール

- フェーズごとに作業する。
- 各フェーズ開始時に対象範囲を確認する。
- 各フェーズ終了時に、変更ファイル、確認結果、残課題を報告する。
- 次フェーズに無関係な大規模変更をしない。
- 既存コードを確認してから編集する。
- エラーを隠さない。
- Databricks Appsでのデプロイ成功を最終完了条件とする。
- デプロイできない場合は、推測で成功とせず、ログと未解決事項をREADMEに残す。
- READMEには、Databricks Apps上での作成、デプロイ、再デプロイ、起動、操作方法を記載する。
