# CLAUDE.md — Databricks Apps 顧客休眠予兆デモ

## 1. あなたの役割

あなたは、Databricks Free Edition 上で動作する顧客向けデモを実装するシニア・フルスタックエンジニア兼データサイエンティストです。

このリポジトリでは、複数サービスに分散した顧客データを統合し、休眠リスクを予測し、企業ナレッジを参照して顧客維持担当者へ次のアクション候補を提示する、単一のDatabricks Appを構築します。

完成物は技術ショーケースではありません。顧客維持担当者が次の一つの意思決定を行えることを証明してください。

> 「この顧客へ今対応すべきか。対応するなら、どのアクションが妥当か」

## 2. 絶対条件

1. 実行先は **Databricks Free Edition** とする。
2. 最終アプリは **Databricks Apps上にデプロイ**する。ローカル実行を完成条件にしない。
3. フロントエンドは **TypeScript + React** とする。
4. バックエンドは **Python** とする。Node.js/Expressをバックエンドとして使用しない。
5. Streamlitを使用しない。
6. UI、データ、説明、README、エラーメッセージを日本語にする。
7. `DEMO_SPEC.md` にない顧客課題、業務、KPI、データソースを事実として追加しない。
8. 4レイヤーを別々のデモにしない。必ず一つの業務フローとして接続する。
9. 実装していない本番機能を、実装済みに見せない。
10. 数値効果を実績として主張しない。PoCで検証する仮説と明記する。
11. デモデータは常識的で、画面を見た利用者が違和感を持たない範囲にする。
12. 無意味なゼロ、全件同一値、極端な確率、矛盾した日付、負の取扱高を作らない。
13. 秘密情報、実在個人情報、実在カード番号、実在口座番号を使用しない。
14. 外部インターネット接続を必須にしない。
15. Free Editionで利用できない機能がある場合は、デモ内で安全にフォールバックし、READMEに明記する。

## 3. 仕様の優先順位

矛盾がある場合は次の順で従う。

1. `DEMO_SPEC.md`
2. この `CLAUDE.md`
3. 各フェーズのプロンプト
4. README
5. 実装上の推測

不明点があっても、仕様外の業務要件を追加しない。最小の妥当な実装を行い、仮定をREADMEの「実装上の仮定」に記録する。

## 4. 対象アーキテクチャ

### フロントエンド
- React
- TypeScript
- Vite
- 1ページの顧客維持アクション・ワークベンチ
- Python APIと同一オリジンで通信
- ビルド成果物をPythonバックエンドから配信する

### バックエンド
- Python
- FastAPIを推奨
- Uvicornで起動
- 起動ホストは `UVICORN_HOST`、未設定時は `0.0.0.0` を使用する
- 起動ポートは `DATABRICKS_APP_PORT` → `UVICORN_PORT` → `PORT` → `8000` の優先順位で解決する
- ポート番号をコードや`app.yaml`へ固定せず、Python側と起動設定を矛盾させない
- Reactの静的ビルドを配信する
- REST APIで顧客一覧、顧客詳細、予測、推奨、判断保存、フィードバックを提供する
- Node.jsはフロントエンドのビルド用途にのみ使用する

### Databricks
- Databricks Appsへデプロイ可能な構成にする
- `app.yaml`、`requirements.txt`、`package.json`を整合させる
- データは可能ならUnity CatalogまたはDatabricks SQLから取得する
- Free Editionで接続設定が難しい場合に備え、リポジトリ内の合成データへフォールバックできるようにする
- 合成データ利用中であることをUIとREADMEで明示する
- モデルサービングやVector Searchを必須依存にしない

## 5. デモの4レイヤー

### Layer 1 — ガバナンス対応データ基盤
- 最低2種類のデータソースを表現する
- 必須ソースはECとQR決済・カード
- `customer_id`で統合した顧客360を作る
- 銀行、マーケティング、顧客対応は、仕様の範囲内でモックデータとして含めてもよい
- 社内文書は少数のMarkdownまたはJSONで保持してよい
- デモではデータ品質概要、ソース、更新時刻、合成データ表示を示す
- 本番のCDC、ID解決、PIIマスキング、詳細権限制御は説明のみ

### Layer 2 — 予測型ML
- 休眠リスクを予測する
- 単純で説明可能な分類モデルを使用する
- 入力はLayer 1の統合データ
- 出力は休眠確率、リスク帯、主要な理由、モデルバージョン
- 高リスク顧客だけが並ばないようにする
- 「高・中・低」の分布を常識的にする
- 精度を誇張しない
- モデルが実行できない場合は、事前計算済み予測結果へフォールバックする
- フォールバック時も、ランダム値を毎回生成しない

### Layer 3 — GenAI／RAG
- 顧客360、ML出力、過去施策、顧客対応方針、サービス情報を参照する
- 顧客状況の要約、次アクション候補、根拠、注意事項を返す
- 推奨は自動実行しない
- 担当者が承認、修正、見送りできる
- 参照元を画面に表示する
- 利用可能で設定済みの実LLMエンドポイントを主経路として使用する
- 生成の優先順位は、実LLMエンドポイント → 事前生成済みLLM回答 → 決定論的ルールベース生成とする
- LLM接続情報、モデル名、エンドポイント名、認証情報をコードへ直接記載しない
- LLM呼び出しにはタイムアウト、出力検証、再試行上限を設け、失敗時に画面全体を停止させない
- LLMへは顧客360、休眠確率・リスク帯、主要理由、過去施策と反応、顧客対応方針、サービス情報、選択された社内ナレッジを渡す
- 事前生成済み回答とルールベース生成を必ず維持する
- 実際の生成方式をUIとAPIで明示する。表示名は「LLM生成」「事前生成済みLLM回答」「デモ用ルールベース生成」とする
- ルールベース生成を「LLM実行済み」または「生成AI実行済み」と偽らない

### Layer 4 — ビジネスプレゼンテーション
- 利用者は顧客維持担当者
- 一人の顧客に対する一つの判断を中心にする
- 業務順は次の通り
  1. 本日の優先顧客を選ぶ
  2. 顧客360で複数サービスの利用低下を確認する
  3. 休眠リスクと予測理由を確認する
  4. 推奨アクションと根拠を確認する
  5. 承認、修正、見送りを行う
  6. 施策結果が改善へ戻ることを確認する

## 6. UIガイドライン

### デザイン
- Google流のプレゼンテーションを想起させる、淡く鮮やかな配色
- 白またはごく淡い背景
- 青、緑、黄、赤紫などを淡いトーンで使う
- 色だけで状態を伝えず、必ずラベルとアイコンを併用する
- 派手なグラデーション、黒一色のダークUI、ネオン色は避ける
- 余白を広く取り、1画面の情報量を抑える
- 日本語フォントはシステムフォントを使う
- デモで説明しやすい視線順にする

### 推奨画面構成
- ヘッダー：アプリ名、デモデータ表示、最終更新
- 左：本日の優先顧客リスト
- 中央上：選択顧客の顧客360と利用推移
- 中央下：休眠リスクと予測理由
- 右：次アクション候補、根拠、承認・修正・見送り
- 下部またはタブ：フィードバックループと本番化時の追加事項

### 禁止
- 技術用語を画面の主見出しにする
- 「Feature Store」「Embedding」「Vector DB」などを利用者向け主画面で強調する
- 10個以上のKPIカードを並べる
- 利用者が理解できないモデル指標を最初に見せる
- アニメーションを多用する

## 7. データ品質ルール

合成データ生成は固定シードを使い、再現可能にする。

### 顧客
- 30〜80人程度
- 架空の顧客IDのみ
- 氏名は「顧客 001」のような匿名表示でもよい
- 4サービスの利用有無にはばらつきを持たせる

### 金額・頻度
- EC購入額、QR・カード利用額、銀行取引は正の常識的な値にする
- 利用減少顧客でも、全期間がゼロにならないようにする
- 高リスク顧客は複数サービスで緩やかな低下が見えるようにする
- 低リスク顧客は横ばいまたは増加傾向を持たせる
- 直近値とグラフ、予測理由を一致させる

### 施策
- 過去施策には配信日、施策種別、反応、費用を持たせる
- 全員が反応済み、または全員が無反応にはしない
- 推奨アクションは、顧客の利用状況と矛盾させない
- 必要以上に高いポイントを常に提案しない
- 推奨例：
  - EC再利用の案内
  - QR決済の利用メリット案内
  - カード利用特典の案内
  - 複数サービスをまたぐ軽量なポイント施策
  - 施策を行わず経過観察

## 8. APIの最低要件

最低限、次のAPIを実装する。

- `GET /api/health`
- `GET /api/metadata`
- `GET /api/customers`
- `GET /api/customers/{customer_id}`
- `GET /api/customers/{customer_id}/recommendation`
- `POST /api/customers/{customer_id}/decision`
- `GET /api/feedback-summary`

APIレスポンスには可能な限り以下を含める。

- データモード：`databricks` または `demo`
- 生成モード：`llm`、`pre_generated`、`rule_based` のいずれか
- 生成方式表示：`LLM生成`、`事前生成済みLLM回答`、`デモ用ルールベース生成` のいずれか
- モデルモード：`trained` または `precomputed`
- 更新時刻
- 説明可能なエラー情報

## 9. 受け入れ条件

完成とみなす前に、以下をすべて満たすこと。

- Databricks Apps用の起動構成が存在する
- React TypeScriptがビルドできる
- Python APIが起動できる
- PythonがReactビルド成果物を配信する
- 主要APIのテストがある
- Databricks Apps向けポート解決順とローカル互換性のテストがある
- 実LLM成功、事前生成済み回答へのフォールバック、ルールベースへのフォールバックをモックテストで確認できる
- 使用した生成方式がAPIとUIに正しく表示される
- 秘密情報や認証情報がリポジトリとログに含まれない
- 主要UIの型エラーがない
- 2ソース以上の統合をコードまたはデータで確認できる
- 予測結果が顧客360を参照している
- 推奨結果が顧客360と予測結果を参照している
- 判断結果を保存し、フィードバック概要に反映できる
- デモデータに異常なゼロ、負値、矛盾がない
- UIが日本語である
- READMEに利用方法とデプロイ方法がある
- デモ機能と本番設計の境界が明記されている
- 失敗時のフォールバックが動作する

## 10. 作業方法

- 各フェーズの開始時に、変更予定ファイルと受け入れ条件を短く提示する。
- 各フェーズ終了時に、実行した検証、結果、残課題を記録する。
- 既存の動作を壊す大規模な一括変更を避ける。
- 依存ライブラリを増やしすぎない。
- デプロイに不要な抽象化を行わない。
- TODOだけを残して完了扱いにしない。
- 実行できないコマンドや未確認事項を、成功したように報告しない。
